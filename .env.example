# Database Configuration
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=deal_finder

# Application URLs
FRONTEND_URL=http://localhost:3001
PORT=3000

# Email Configuration (Optional - for email verification and password reset)
# For development, you can use ethereal.email or leave blank to log emails to console
# For production, use a real SMTP service (SendGrid, AWS SES, etc.)
NODE_ENV=development
EMAIL_FROM=noreply@dealfinder.com
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_ethereal_email
SMTP_PASS=your_ethereal_password
